"""Reserved yt-dlp extractor-plugin namespace.

Add only minimal, documented upstream compatibility overrides here. Each active
override must also be represented by services.compatibility metadata so its
tested version window and upstream reference are visible and removable.

There are intentionally no permanent site-specific extractors in this package.
"""
